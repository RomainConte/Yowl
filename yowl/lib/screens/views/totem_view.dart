import 'package:flutter/material.dart';

class TotemView extends StatelessWidget {
  const TotemView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Home'),);
  }
}